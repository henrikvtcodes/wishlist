{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun svelte-kit sync && bun convex codegen && bun run build"
  },
  "deploy": {
    "startCommand": "node build/index.js"
  }
}